#!/usr/bin/bash
python2 -m nltk.downloader punkt stopwords averaged_perceptron_tagger
#DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
#sudo ln -s $DIR/nn_search2.py /usr/local/bin/nn_search2
